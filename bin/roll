#!/usr/bin/env ruby
require 'rolls' unless defined?(::Roll)

ARGV.shift if ARGV.first == '-'

begin
  Roll::Shell.main(*ARGV)
rescue => err
  raise err if $DEBUG
  $stderr.puts(err.message)
  exit -1
end

