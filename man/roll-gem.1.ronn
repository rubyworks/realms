roll-gem(1) -- run a gem command and re-sync
============================================

## SYNOPSIS

`roll-gem` <gem-command-arguments>

## DESCRIPTION

`roll-gem` simply passes teh argument given it on to the `gem` command.
After successful completion of which roll automatically resyncs any
environment that is no longer current.

## SEE ALSO

roll(1), roll-sync(1),
<http://proutils.github.com/roll>

