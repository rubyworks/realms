realm-list(1) -- list available libraries in load environment
=============================================================

## SYNOPSIS

`realm-list`

## DESCRIPTION

`realm-list` prints a multi-column list of the names of the available libraries
in the current roll environment.

## OPTIONS

  * `-v`/`--verbose` - Prodoces a detailed list of name, version, release date
    and path for each library.

## SEE ALSO

realm(1)
