roll-sync(1) -- synchronize an environment index
================================================

## SYNOPSIS

`roll-sync` [<name>]

## DESCRIPTION

`roll-sync` synchronizes an environment's index to it's lookup list. Without
a specific `<name>`, `roll-sync`will re-sync every environment that is not
current.

## SEE ALSO

roll(1),
<http://proutils.github.com/roll>

